{
  "buildCommand": "npm install --legacy-peer-deps --ignore-scripts && vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts"
}
